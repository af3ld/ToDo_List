<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <TextView
        android:id="@+id/textView"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:textAlignment="center"
        android:textSize="22sp"
        android:layout_marginTop="15dp"
        android:text="@string/textview"
        android:textColor="@color/softLavender"
        />

    <EditText
        android:id="@+id/edit_text_main"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        android:layout_weight="7"
        android:textAlignment="center"
        android:inputType="textAutoComplete|textCapSentences|textAutoCorrect"
        />

    <View
        android:layout_width="match_parent"
        android:layout_height="2dp"
        android:background="@color/intriguingRed"
        android:layout_marginBottom="5dp"
        android:layout_marginTop="5dp">
    </View>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:orientation="horizontal">

        <Button
            android:id="@+id/main_activity_button_left"
            android:text="@string/outerButtonTitle"
            style="@style/appButtonTheme"/>

        <Button
            android:id="@+id/main_activity_button_middle"
            android:text="@string/midButtonTitle"
            style="@style/appButtonTheme"/>

        <Button
            android:id="@+id/main_activity_button_right"
            android:text="@string/outerButtonTitle"
            style="@style/appButtonTheme"/>
    </LinearLayout>


</LinearLayout>